<!-- CONTENT -->

<div class="title-h-center">
    <div class="title-d">
        <div><span>Полиграфия</span></div>
    </div>
    <div class="text">Мои достижения и подтверждения проффесионализма</div>
</div>

<div class="container">
  <div class="row">
      <% Book.all.each do |book| %>
        <div class="col-lg-12">
          <div class="b-polygraphy shadow col-lg-4">
              <div class="book" id=<%= "book-#{book.id}" %>>
                  <%= image_tag(book.image.url) %>
              </div>
          </div>
          <div class = 'content col-lg-8'>
              <h3><%= book.title %></h3>
              <p> <%= book.content %></p>
              <div class = 'col-lg-4', style= 'padding: 20px 0;'>
                <%= link_to 'купить в Украине', book.buy_link, class: "btn btn-3", target: '_blank' %>
              </div>
              <div class = 'col-lg-4', style= 'padding: 20px 0;'>
                <%= link_to 'купить в электронном виде', book.buy_e_link, class: "btn btn-3", target: '_blank' %>
              </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
