$('.inst-posts').append('<%= escape_javascript render partial: 'post', collection: @posts %>');
$('#load-more-link').replaceWith("<%= escape_javascript(link_to_next_page(@posts, 'посмотреть больше статей', :remote => true, :id=>'load-more-link'))%>");
 <% if @posts.last_page? %>
   $('.fa-refresh').hide();
<% end %>
