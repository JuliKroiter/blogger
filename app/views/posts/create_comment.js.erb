$('.comments').append("<%= escape_javascript(render(:partial => 'comment', :locals => {:comment => @comment})) %>")
$('textarea').val('')
$('#create_commenter, #comment_commenter').val('')
$('#show-comments span').text("<%= @post.comments.count %>")
$('#comments-count-' + "<%=@post.id%>").text("<%= @post.comments.count %>")
$('#count-' + "<%=@post.id%>").text("<%= @post.comments.count %>")
