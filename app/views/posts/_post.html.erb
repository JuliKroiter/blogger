<div class="col-lg-6">
  <p><%= image_tag post.image_url(:thumb) %></p>
  <p><%= post.title %></p>
  <p>#<%= post.category.try(:title) %></p>
  <p><%= truncate post.content, length: 160 %></p>
  <p>
    <%= fa_icon('comment') %>
    <%= post.comments.length%>
  </p>
</div>