<%= content_for :header do %>
  Comment №
  <%= @comment.id %>
<% end %>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <%= @comment.id %>
      </div>

      <div class="panel-body">
        <%= @comment.content %>

        <%= link_to 'Edit', edit_admin_comment_path(@comment), class: 'btn btn-default' %>
      </div>
    </div>
  </div>
</div>
