<%- if @post %>
  <p>New comment</p>

  <%= form_for [:admin, @post, @post.comments.new] do |f| %>
    <div class="field">
      <%= f.label :content %><br>
      <%= f.text_field :content %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<% end %>